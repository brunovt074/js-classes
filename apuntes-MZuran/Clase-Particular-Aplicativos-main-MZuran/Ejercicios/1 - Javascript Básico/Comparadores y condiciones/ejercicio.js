console.log("Soy la consola! Encontra cómo resolver el ejercicio en ejercicio.js")

/*
    Esta línea que tenemos acá abajo sirve para preguntarle a el usuario un número a ingresar.
    Descomentala para usarla.

    La función Number() transforma el parámetro enviado a un número. prompt() nos devuelve un string
    ingresado por el usuario y necesitamos un número, así que lo convertimos.
*/
//const numero = Number(prompt("Ingresa un número:"));
//console.log("El usuario ingresó el valor " + numero + " cuyo tipo de dato es " + typeof(numero))